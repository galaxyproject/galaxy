<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE visualization SYSTEM "../../visualization.dtd">
<visualization name="Tiff Viewer" embeddable="true">
    <description>Tiff Image Viewer</description>
    <tags>
        <tag>TIFF</tag>
        <tag>Image</tag>
    </tags>
    <data_sources>
        <data_source>
            <model_class>HistoryDatasetAssociation</model_class>
            <test test_attr="ext">tiff</test>
        </data_source>
    </data_sources>
    <params>
        <param required="true">dataset_id</param>
    </params>
    <entry_point entry_point_type="script" src="script.js" css="script.css"/>
    <tests>
        <test>
            <param name="dataset_id" value="http://cdn.jsdelivr.net/gh/galaxyproject/galaxy-test-data/1.tiff" ftype="tiff" />
        </test>
    </tests>
    <help format="markdown"><![CDATA[
# What is the TIFF Viewer?

The TIFF Viewer provides a simple way to display TIFF images directly in your browser.

After selecting a dataset, the TIFF image will be shown. No special configuration or adjustments are needed — it's a lightweight, easy-to-use viewer.

## Key Features

- **Display**: View TIFF images quickly without external software.
- **Basic Interaction**: Pan and zoom to explore details.

This tool is ideal for reviewing microscopy images, scanned documents, or any standard TIFF data.
    ]]></help>
</visualization>