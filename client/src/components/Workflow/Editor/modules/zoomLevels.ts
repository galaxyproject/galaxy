// Re-export from shared location
export type { ZoomLevel } from "@/utils/zoomLevels";
export {
    getSnappedZoom,
    getZoomInLevel,
    getZoomOutLevel,
    isMaxZoom,
    isMinZoom,
    maxZoom,
    minZoom,
    zoomLevels,
} from "@/utils/zoomLevels";
