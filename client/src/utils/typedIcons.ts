import type { IconDefinition } from "font-awesome-6";

// typecasts to make icon types compatible with font awesome icon component

export const galaxyLogo = ["gxd", "galaxyLogo"] as unknown as IconDefinition;
export const textSmaller = ["gxd", "textSmaller"] as unknown as IconDefinition;
export const textLarger = ["gxd", "textLarger"] as unknown as IconDefinition;
