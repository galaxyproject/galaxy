class: GalaxyWorkflow
inputs:
  input:
    type: data
outputs:
  output:
    outputSource: the_step/output
steps:
  the_step:
    tool_id: gx_data
    tool_version: "1.0.0"
    in:
      parameter: input
