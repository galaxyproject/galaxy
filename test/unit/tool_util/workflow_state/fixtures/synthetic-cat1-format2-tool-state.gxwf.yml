class: GalaxyWorkflow
label: Synthetic Cat1 Format2 tool_state
inputs:
  input_dataset:
    type: data
steps:
  concat:
    tool_id: cat1
    tool_version: "1.0.0"
    in:
      input1:
        source: input_dataset
    tool_state:
      queries: []
outputs:
  concatenated:
    outputSource: concat/out_file1
