class: GalaxyWorkflow
inputs:
  input_collection:
    type: collection
    collection_type: list:list
steps:
  step1:
    tool_id: collection_list_paired_or_unpaired
    in:
      f1: input_collection
