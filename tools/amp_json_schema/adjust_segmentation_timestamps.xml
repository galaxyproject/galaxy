<tool id="adjust_segmentation_timestamps" name="Adjust Segmentation Timestamps" version="1.0.0">
  <description>Adjust timestamps so Segmentation output matches timestamps of the original file</description>
    <requirements>
        <requirement type="package" version="8.30">coreutils</requirement>
    </requirements>
  <command detect_errors="exit_code">$__tool_directory__/adjust_segmentation_timestamps.py $segmentation_json $adj_json $amp_segmentation</command>
   <inputs>
   <!-- should be wave data type -->
    <param name="segmentation_json" type="data" format="json" label="AMP Segmentation JSON From Speech to Text" help="Diarization output in the form of AMP Segmentation output"/>
    <param name="adj_json" type="data" format="json" label="Kept segments JSON From Remove Segments" help="List of kept segments"/>
  </inputs>
  <outputs>
    <data format="json" name="amp_segmentation" />
  </outputs>
  <tests>
  </tests>
  <help>
.. class:: infomark

Adjust timestamps of a segmentation json from speaker diarization output to match timestamps of the original file.

  </help>
</tool>
