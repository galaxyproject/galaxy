<tool id="scene_detection" name="PySceneDetect Scene Detection" version="1.0.0">
  <description>Runs scene detection on a video</description>
    <requirements>
    </requirements>
  <command detect_errors="exit_code"> $__tool_directory__/scene_detection.py $input $threshold $amp_scenes $scene_detect_stats</command>
  <inputs>
	<param name="input" type="data" format="video" label="Video File" help="A video file input for scene detection"/>
	<param name="threshold" type="integer" label="(Optional) Threshold" value="30" optional="true" help="Sensitivity of the scene detection. The default is 30"/>
  </inputs>
  <outputs>
    <data format="json" name="amp_scenes" />
    <data format="csv" name="scene_detect_stats" />
  </outputs>
  <help>
.. class:: infomark

Run PySceneDetect on a video to create a list of scenes

  </help>
</tool>
