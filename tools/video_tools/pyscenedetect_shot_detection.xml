<tool id="shot_detection" name="PySceneDetect" version="1.0.0">
  <description>Runs shot detection on a video</description>
    <requirements>
    </requirements>
  <command detect_errors="exit_code"> $__tool_directory__/pyscenedetect_shot_detection.py $input $threshold $amp_shots $frame_stats</command>
  <inputs>
	<param name="input" type="data" format="video" label="Video File" help="A video file input for shot detection"/>
	<param name="threshold" type="integer" label="(Optional) Threshold" value="30" optional="true" help="Sensitivity of the shot detection. The default is 30"/>
  </inputs>
  <outputs>
    <data format="json" name="amp_shots" />
    <data format="csv" name="frame_stats" />
  </outputs>
  <help>
.. class:: infomark

Run PySceneDetect on a video to create a list of shots

  </help>
</tool>
