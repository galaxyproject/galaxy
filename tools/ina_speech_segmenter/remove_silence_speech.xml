<tool id="remove_silence_speech" name="Remove Silence and Speech" version="1.0.0">
  <description>Create an audio file containing only music, removing silence and speech</description>
    <requirements>
    </requirements>
  <command detect_errors="exit_code">
  $__tool_directory__/remove_silence_music_speech.py $input $input_segmentation_json speech $segmented_audio_file $amp_kept_segments</command>
   <inputs>
   <!-- should be wave data type -->
    <param name="input" type="data" format="audio" label="Original Audio" help="An audio file"/>
    <param name="input_segmentation_json" type="data" format="segment" label="Segments in the Audio" help="Segments generated from Segmentation"/>
  </inputs>
  <outputs>
    <data name="segmented_audio_file" format="music" label="Audio with only Music Segments"/>
    <data name="amp_kept_segments" format="json" label="Kept Segments JSON" />
  </outputs>
  <tests>
  </tests>
  <help>
.. class:: infomark

Remove segments of silence and speech from an audio file.

  </help>
</tool>
